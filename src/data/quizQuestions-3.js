
export const quizQuestions_3 = {
  questions: [
    {
      question: 'What is the purpose of the .map() method in JavaScript?',
      choices: [
        'To create a new array with the results of calling a provided function on every element',
        'To filter the elements of an array based on a given condition',
        'To find the first element in an array that satisfies a given condition',
        'To concatenate two arrays into a single array',
      ],
      correctAnswer: 'To create a new array with the results of calling a provided function on every element',
    },
    {
      question: 'How does the .filter() method work in JavaScript?',
      choices: [
        'It creates a new array with the results of calling a provided function on every element',
        'It filters the elements of an array based on a given condition',
        'It finds the first element in an array that satisfies a given condition',
        'It concatenates two arrays into a single array',
      ],
      correctAnswer: 'It filters the elements of an array based on a given condition',
    },
    {
      question: 'What does the .find() method return in JavaScript?',
      choices: [
        'An array containing the elements that satisfy a given condition',
        'The first element in an array that satisfies a given condition',
        'A boolean indicating whether any element in the array satisfies a given condition',
        'A new array with the results of calling a provided function on every element',
      ],
      correctAnswer: 'The first element in an array that satisfies a given condition',
    },
    {
      question: 'How can you use the .map() method to double each element in an array?',
      choices: [
        'array.map(element => element * 2)',
        'array.map(element => element + element)',
        'array.map(element => element / 2)',
        'array.map(element => element - element)',
      ],
      correctAnswer: 'array.map(element => element * 2)',
    },
    {
      question: 'In the context of .filter(), how can you filter out all even numbers from an array?',
      choices: [
        'array.filter(element => element % 2 === 0)',
        'array.filter(element => element % 2 !== 0)',
        'array.filter(element => element % 2 === 1)',
        'array.filter(element => element % 2 !== 1)',
      ],
      correctAnswer: 'array.filter(element => element % 2 === 0)',
    },
    {
      question: 'What is the purpose of the callback function provided to the .find() method?',
      choices: [
        'To create a new array with the results of calling a provided function on every element',
        'To filter the elements of an array based on a given condition',
        'To find the first element in an array that satisfies a given condition',
        'To concatenate two arrays into a single array',
      ],
      correctAnswer: 'To find the first element in an array that satisfies a given condition',
    },
    {
      question: 'How can you use the .map() method to transform an array of strings to uppercase?',
      choices: [
        'array.map(element => element.toLowerCase())',
        'array.map(element => element.toUpperCase())',
        'array.map(element => element.charAt(0).toUpperCase() + element.slice(1))',
        'array.map(element => element.charAt(0).toLowerCase() + element.slice(1))',
      ],
      correctAnswer: 'array.map(element => element.toUpperCase())',
    },
    {
      question: 'In the context of .filter(), how can you filter out all elements shorter than 5 characters from an array of strings?',
      choices: [
        'array.filter(element => element.length >= 5)',
        'array.filter(element => element.length < 5)',
        'array.filter(element => element.length === 5)',
        'array.filter(element => element.length !== 5)',
      ],
      correctAnswer: 'array.filter(element => element.length >= 5)',
    },
    {
      question: 'What does the .find() method return if no element in the array satisfies the given condition?',
      choices: [
        'An empty array',
        'null',
        'undefined',
        '0',
      ],
      correctAnswer: 'undefined',
    },
    {
      question: 'How can you use the .map() method to extract the lengths of strings from an array of strings?',
      choices: [
        'array.map(element => element.length)',
        'array.map(element => element.size)',
        'array.map(element => element.count)',
        'array.map(element => element.characters)',
      ],
      correctAnswer: 'array.map(element => element.length)',
    },  
  ],
}